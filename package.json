{
  "name": "booking-inventory-patterns",
  "version": "0.1.0",
  "description": "Executable reference implementations of concurrency and consistency bugs in booking/inventory systems. Each pattern proves a real race with a failing test, then fixes it and proves the fix with the same test.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Julen Galera <julen.galera.dev@gmail.com>",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "prettier": "^3.9.6",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
